It's the closest RLS currently to do what I want, which means spinning up sub-agents autonomously so I shouldn't have to ask. I feel always blocked like in a sequential regardless the tool I use, even with cloud code.