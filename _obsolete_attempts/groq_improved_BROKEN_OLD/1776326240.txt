All right, what you are doing is completely messy and shitty. So can you get back to when we had the two MD files for this branch, removing the different shell scripts that you created? And what we're going to do instead of using Westterm or anything is that we will just spawn one terminal with GitHub Copilot CLI where we will ask to address the six different issues with six sub-agents, but with the whole funnel of verification at the end for each of the sub-agents and so on.