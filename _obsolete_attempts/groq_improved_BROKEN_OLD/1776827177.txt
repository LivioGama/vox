Didn't you forget to clean? Also, I see a lot of... ...`tiff`... ...added files, check they are all legit. I don't understand why there is a `payments.js` on server if we actually moved it to a `root.js`.