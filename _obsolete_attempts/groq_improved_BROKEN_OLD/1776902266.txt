Alright, so note all the changes that you have to do. First of all, when you are generating a new project and that there is the sound and the animation, in parallel you should generate the image and start displaying them in the prompt animation view. Then, at the moment we receive the first chunk, you should display the preview browser studio and there should not be any space below the studio like it's currently. Then it should start streaming and displaying the element inside, which means actually we don't wait anymore. The home page is finished to display the preview, we actually display it immediately when we receive the first chunk. Then some other things that you have to do is when I click back, I should immediately go back to the form. Currently it actually is not working and it's not fluid enough. It should already be loaded behind and then you have a lot of optimization to do because it takes a lot of time to load the session page. It takes a lot of time to load also the previously generated. All of this should be kind of cached and immediate to display. So work on those different points using 20 parallel sub-agents to go faster.