SSH into the MacBook, Claudia, on the network and then you will see it's a Debian version freshly installed. The user is Livio, the password is Lolo. You should copy there my free SSH key. You should also copy my OVPN files which is called profile backup like this. Check some of the time that I'm using according to the modified or intent and then you should install OpenVPN on there. You should connect the VPN. You should also configure, check out the directory that is called Unix config from my GitHub. You should also configure the SSH-conf just like on my macOS because I have a specific port to access some machine. I'm talking about the Mac Mini, the Pro and so on. I also need you to copy my ZSH-RC. I mean it will be restored through the Unix config so it's fine. But you have to copy my ZSH history to have the list of command line I'm using. Don't forget to start actually by installing Oh My ZSH because you won't be able to have the list of command line I was using. Don't forget to start actually by installing uh Oh My ZSH because you won't be able to have the session right. It's really required to install it offline. But you need to do something really agentic until the end. And also I would encourage that you uh yeah, you should also install Node. You should install you should install a Copilot API which is a proxy that will allow to target the YouTube Copilot models locally. And you should configure also the... you should also install Cloud CLI and you should configure the cloud setting global in order so they target the Copilot API. But do that after the check-in and the configuration of Uniteconfig because otherwise it will be overridden. Then when you reach that stage and that you have pretty much everything set up, I invite you to try to check out the repository that is called server apps. If you don't know where it is, you can check easily by SSH-ing into the FNC62 VPS and you will find it somewhere in one of the services.