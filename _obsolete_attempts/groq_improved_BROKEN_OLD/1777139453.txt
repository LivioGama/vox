I would like you to create a harness that will take advantage of Westerm or cmux. For now, we focus on Westerm and the multiple panes it's can spawn. This harness will be able to have, in the first place, an orchestrator or a planner that will split the task in a number of tasks, and this those tasks will be each treated separately in a pane. As depends the pan of Westerm, using all those pans will be using the SWE 1.6 fast model, which which is normally only inside Winesurf, but I found a way to expose it's through a proxy, and the address of this proxy is already self-hosted on my VPS, and this address is