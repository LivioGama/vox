Build for me yet one of the millions of ED and RNS that's released in the world. This one will be able based on the fact that the user has an input that will be a sheet that will pass through an orchestrator and a planner that will decide how to split the task and decide how much terminals that will be spawned. For this we will use WazeTerm because it's multi-panes. We want to same the feature that spawns in Grid. And since SWE 1.6 fast is just a model, we need a real harness to use it in WazeTerm. So for that we will take a generic one, like OpenCode, configure it to run the task.